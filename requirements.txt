# Fase 1 corre solo con stdlib + python-dotenv (IMAP y parseo de correo son stdlib).
# El resto se activa a medida que avanzan las fases (import perezoso, no rompe si falta).
python-dotenv>=1.0.0

# Orquestación: grafo de estados que procesa cada correo (clasificar→…→despachar).
langgraph>=1.0

# Agentes LLM (clasificador L2 con salida estructurada + redactor coloquial) sobre OpenAI.
langchain-core>=1.0
langchain-openai>=1.0

# L2 alternativo con Claude (opcional; el camino por defecto es OpenAI vía LangChain).
anthropic>=0.39.0

# Persistencia en Supabase/Postgres — Fase 2
psycopg[binary]>=3.1

# Extracción de texto de PDFs adjuntos — Fase 2
pypdf>=4.0

# Servicio + scheduler (polling IMAP, recordatorios, retención) — Fase 1/3
fastapi>=0.111
uvicorn>=0.30
APScheduler>=3.10
